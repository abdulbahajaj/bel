(set mac
    (lit mac
        (lit clo scope (name args body)
            `(set ,name
                (lit mac
                    (lit clo scope ,args ,body))))))

(mac fn (args body)
    `(lit clo ,scope ,args ,body))

(mac def (name args body)
    `(set ,name (fn ,args ,body)))

(def add-one (a) (fn () a))

(set x (add-one 1010))

(prn x)

(prn (x))

